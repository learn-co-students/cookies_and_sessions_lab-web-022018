<h2>Cart:</h2>
<ul>
  <% cart.each do |item| %>
    <li><%= item %></li><br>
  <% end %>
</ul>


<%= form_for :product do |f| %>
<%= f.label :product %>
<%= f.text_field :name %>
<%= f.submit "add to cart"%>
<% end %>
